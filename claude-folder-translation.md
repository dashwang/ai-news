# .claude/ 文件夹的完整解剖结构

大多数团队都以某种形式采用了人工智能，但"使用人工智能"和"从人工智能中获得可衡量的投资回报率"之间的差距比人们意识到的要大。

Claude Code 用户通常将 .claude 文件夹视为黑匣子。他们知道它存在。他们已经看到它出现在项目根目录中。但他们从未打开过它，更不用说了解其中每个文件的用途了。

那是一个错失的机会。

**.claude 文件夹是 Claude 在项目中行为方式的控制中心。**

它保存着你的指令、自定义命令、权限规则，甚至是 Claude 跨会话的记忆。一旦你了解了什么存在于何处以及为什么存在，你就可以配置 Claude Code，使其完全按照团队需要的方式运行。

本 Newsletter 将引导你了解该文件夹的整个结构，从每天都会使用的文件到设置一次后就会忘记的文件。

在深入研究之前，需要先了解一件事：**实际上有两个 .claude 目录，而不是一个。**

第一个位于你的项目中，第二个位于你的主目录中：

- **项目级文件夹**保存团队配置。你将其提交给 git。团队中的每个人都获得相同的规则、相同的自定义命令、相同的权限策略。
- **全局 ~/.claude/** 文件夹保存你的个人偏好和计算机本地状态，例如会话历史和自动记忆。

---

## CLAUDE.md — 最重要的文件

这是整个系统中最重要的文件。当你启动 Claude Code 会话时，它首先读取的就是 CLAUDE.md。它直接加载到系统提示中，并在整个对话中记住它。

简单来说：**你在 CLAUDE.md 中写什么，Claude 就会遵循什么。**

如果你告诉 Claude 总是在实现之前编写测试，它会的。如果你说"永远不要使用 console.log 进行错误处理，始终使用自定义日志模块"，它每次都会尊重这一点。

项目根目录下的 CLAUDE.md 是最常见的设置。但你也可以在 ~/.claude/CLAUDE.md 中设置一个用于适用于所有项目的全局首选项，甚至可以在子目录中设置一个用于特定于文件夹的规则。Claude 会把它们全部读取并结合起来。

大多数人要么写得太多，要么写得太少。以下是有效的方法：

**写：**
- 构建、测试和 lint 命令（npm run test、make build 等）
- 关键架构决策（"我们使用 Turborepo 的 monorepo"）
- 不明显的陷阱（"TypeScript 严格模式已开启，未使用的变量是错误"）
- 导入约定、命名模式、错误处理风格
- 主要模块的文件和文件夹结构

**不要写：**
- 属于 linter 或格式化程序配置的任何内容
- 可以直接链接到的完整文档
- 解释理论的冗长段落

**保持 CLAUDE.md 在 200 行以内。** 超过这个长度的文件会消耗太多上下文，Claude 的指令遵从性反而会下降。

这是一个简单但有效的例子，大约 20 行。它给了 Claude 在这个代码库中高效工作所需的一切，无需不断澄清。

---

## CLAUDE.local.md — 个人偏好

有时候你有一个特定于你而不是整个团队的偏好。也许你更喜欢不同的测试运行器，或者希望 Claude 始终使用特定模式打开文件。

在项目根目录创建 CLAUDE.local.md。Claude 会与主 CLAUDE.md 一起读取它，并且它会自动被 git 忽略，这样你的个人调整永远不会出现在代码库中。

---

## rules/ — 拆分指令

CLAUDE.md 对单个项目很好用。但一旦你的团队成长，你会得到一个 300 行的 CLAUDE.md，没有人维护，每个人都忽略。

**rules/ 文件夹**解决了这个问题。

.markdown 文件夹内的每个 .claude/rules/ 都会自动与你的 CLAUDE.md 一起加载。与其是一个巨大的文件，不如按关注点拆分指令：

每个文件保持专注且易于更新。负责 API 约定的团队成员编辑 api-conventions.md。负责测试标准的人编辑 testing.md。没有人互相干扰。

真正的力量来自**路径范围规则**。在规则文件中添加 YAML frontmatter 块，它只在与匹配的文件一起工作时才会激活：

当编辑 React 组件时，Claude 不会加载此文件。它只在 src/api/ 或 src/handlers/ 中工作时才加载。没有 paths 字段的规则无条件加载，每个会话都加载。

这是当你的 CLAUDE.md 开始变得拥挤时的正确模式。

---

## commands/ — 自定义命令

开箱即用的 Claude Code 有内置的斜杠命令如 /help 和 /compact。**commands/ 文件夹**让你添加自己的命令。

你放入 .claude/commands/ 的每个 markdown 文件都会变成一个斜杠命令。

名为 review.md 的文件创建 /project:review。名为 fix-issue.md 的文件创建 /project:fix-issue。文件名就是命令名称。

这是一个简单的例子。创建 .claude/commands/review.md：

现在在 Claude Code 中运行 /project:review，它会在 Claude 看到之前自动将真正的 git diff 注入到提示中。！反引号语法运行 shell 命令并嵌入输出。这才是这些命令真正有用的地方，而不仅仅是保存的文本。

使用 $ARGUMENTS 在命令名称后传递文本：

运行 /project:fix-issue 234 会将 issue 234 的内容直接输入提示。

项目命令在 .claude/commands/ 中提交并与你的团队共享。对于你希望在所有项目中都能使用的命令，将它们放在 ~/.claude/commands/ 中。这些显示为 /user:command-name 而不是 /project:command-name。

一个有用的个人命令：每日站会助手、生成遵循你的约定的提交消息的命令，或快速安全扫描。

---

## Skills vs Commands

你现在知道命令是如何工作的。Skills 表面上看似相似，但触发方式完全不同。以下是我们继续之前的区别：

**Commands** 是你输入斜杠命令时执行的。**Skills** 是当任务匹配技能描述时，Claude 可以自行调用而不需要你输入斜杠命令。命令等待你。Skills 观察对话并在时机成熟时行动。

每个 skill 都驻留在自己的子目录中，包含一个 SKILL.md 文件：

SKILL.md 使用 YAML frontmatter 来描述何时使用它：

当你说"审查这个 PR 的安全问题"时，Claude 读取描述，识别它匹配，并自动调用技能。你也可以用 /security-review 显式调用它。

与命令的关键区别：**Skills 可以打包支持文件。** 上面的 DETAILED_GUIDE.md 引用拉取了一个位于 SKILL.md 旁边的详细文档。命令是单个文件。Skills 是包。

个人技能放在 ~/.claude/skills/ 中，在所有项目中可用。

---

## agents/ — 子代理

当任务复杂到值得专门的专家时，你可以在 .claude/agents/ 中定义一个子代理角色。每个代理是一个 markdown 文件，有自己的系统提示、工具访问和模型偏好：

当需要代码审查时，Claude 会在自己的隔离上下文窗口中生成这个代理。代理完成工作，压缩发现，然后报告回来。你的主会话不会被数千个探索 token 弄乱。

**tools 字段**限制代理可以做什么。安全审计员只需要 Read、Grep 和 Glob。它没有理由写文件。这种限制是故意的，值得明确说明。

**model 字段**让你使用更便宜、更快的模型来完成专注的任务。Haiku 处理大多数只读探索很好。把 Sonnet 和 Opus 留给真正需要它们的工作。

个人代理放在 ~/.claude/agents/ 中，在所有项目中可用。

---

## settings.json — 权限控制

.claude/ 内的 settings.json 文件控制 Claude 允许和不允许做什么。它定义 Claude 可以运行哪些工具，可以读取哪些文件，以及在运行某些命令之前是否需要询问。

完整文件看起来像这样：

**$schema 行**在 VS Code 或 Cursor 中启用自动完成和内联验证。始终包含它。

**allow 列表**包含无需 Claude 询问即可运行的命令。对于大多数项目，一个好的 allow 列表涵盖：

- Bash(npm run *) 或 Bash(make *) 让 Claude 可以自由运行脚本
- Bash(git *) 用于只读 git 命令
- Read、Write、Edit、Glob、Grep 用于文件操作

**deny 列表**包含完全阻止的命令，无论什么。明智的 deny 列表阻止：

- 破坏性 shell 命令如 rm -rf
- 直接网络命令如 curl
- 敏感文件如 .env 和 secrets/ 中的任何内容

如果东西不在任一列表中，Claude 会在继续之前询问。中间地带是有意的。它给你一个安全网，而不必预判每一种可能的命令。

你也可以有 settings.local.json 用于个人覆盖。它与 CLAUDE.local.md 有相同的想法。创建 .claude/settings.local.json 用于你不想提交的权限更改。它自动被 git 忽略。

---

## ~/.claude/ — 全局配置

你不会经常与这个文件夹交互，但知道里面有什么很有用。

- **~/.claude/CLAUDE.md** 加载到每个 Claude Code 会话中，跨越所有你的项目。是放置个人编码原则、首选样式或任何你想让 Claude 记住的东西的好地方，无论你在哪个代码库中。

- **~/.claude/projects/** 存储每个项目的会话记录和自动记忆。Claude Code 自动保存它自己的笔记：它发现的命令、观察到的模式，以及架构见解。这些跨会话持久存在。你可以用 /memory 浏览和编辑它们。

- **~/.claude/commands/** 和 **~/.claude/skills/** 持有在所有项目中可用的个人命令和技能。

你通常不需要手动管理这些。但知道它们存在会很方便，当 Claude 似乎"记得"你从未告诉过它的东西，或者当你想要清除项目的自动记忆并重新开始时。

---

## 从零开始：推荐的进步路径

如果你从零开始，这里有一个很好的进步路径：

**第一步**：在 Claude Code 中运行 /init。它通过读取你的项目生成一个启动 CLAUDE.md。把它编辑到基本要旨。

**第二步**：添加 .claude/settings.json，包含适合你的技术栈的 allow/deny 规则。至少，允许你的运行命令并拒绝 .env 读取。

**第三步**：为你最常做的工作流创建一两个命令。代码审查和 issue 修复是很好的起点。

**第四步**：随着你的项目增长，你的 CLAUDE.md 变得拥挤，开始将指令拆分到 .claude/rules/ 文件中。在有意义的地方按路径限定范围。

**第五步**：添加 ~/.claude/CLAUDE.md 与你的个人偏好。这可能是"总是在实现之前写类型"或"喜欢函数式模式而不是基于类"这样的东西。

这确实是 95% 项目所需要的全部。技能和代理在你有值得打包的重复复杂工作流时才会出现。

---

## 结语

.claude 文件夹实际上是一个协议，用于告诉 Claude 你是谁，你的项目做什么，以及它应该遵循什么规则。你越清楚地定义这一点，你花在纠正 Claude 上的时间就越少，它花在有用工作上的时间就越多。

**CLAUDE.md 是你最高杠杆的文件。先把它弄对。其他都是优化。**

从小处开始，随着时间的推移进行改进，并像对待项目中的任何其他基础设施一样对待它：一旦设置正确，它每天都会产生红利。

感谢阅读！
