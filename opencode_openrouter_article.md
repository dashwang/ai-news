# 在中国白嫖AI编程工具的正确姿势

最近这半年，程序员圈的吐槽越来越多了。Claude Code开始收费，GitHub Copilot年年涨价，那些所谓的"免费"编程工具，要么限速要么限流，用起来一肚子火。但大多数人不知道的是，老外们早就不在这棵树上吊死了。他们用的一套组合拳，在国内还没火起来，但真的香。

这套组合叫OpenCode加OpenRouter。OpenCode是一个开源的AI编程助手，GitHub上12万颗星，每月500万开发者再用。你可以把它理解成终端里的Claude Code，直接在命令行让它帮你写代码、改bug、重构项目。更重要的是，它开源透明，你的数据完全可控。OpenRouter则是AI模型的统一API网关，75家 provider的上百种模型，一个key就能全部调用，而且经常有免费额度。

把这俩组合在一起，你每个月可以不花一分钱，就拥有和付费订阅一样的体验。听起来不可能？但这是真的。而且能玩的花样，比那些闭源付费工具多得多。

第一种玩法是模型轮换。你可以同时配GPT-4、Claude、Gemini、Llama四种模型，需要写代码时用Claude，需要推理时用GPT，需要省钱时用开源模型。OpenRouter会自动选最优路径，同一个任务，不同模型费用能差好几倍。有人测试了一个2000行代码重构，GPT-4o要2美元，Claude 3.5只要0.8美元，开源的Llama 4居然只要0.1美元。三个月省下的钱够吃好几顿火锅。

第二种玩法是白嫖免费额度。OpenRouter每个月都会放新模型免费试用，勤快关注官方动态，每个月能蹭好几百块的额度。用家人朋友多注册几个账号，一个家庭一个月白嫖上千块不是问题。当然，这是建立在真实需求上，商业项目还是得正常付费。

第三种玩法是本地加API混合。本地跑一个7B模型处理简单任务，复杂任务才调用商业API。有开发者用这方法，把每月支出从2000块降到300块，性能几乎没损失。配置好了之后，AI会自动判断什么时候用本地，什么时候用远程。

第四种玩法是自定义提示词。把你的代码规范、命名习惯、常用库都喂给OpenCode，它生成的代码几乎不用改。GitHub上现成的提示词库一堆，照着改就行。进阶玩家还能针对不同场景设置不同提示词，重构用一套，写测试用另一套。

第五种玩法是自动化工作流。让OpenCode帮你review代码、生成文档、写测试、提交PR。有人把它和GitHub Actions绑在一起，每次push自动review，没问题自动合并，省了好几个小时的code review时间。适合那些每天要处理大量PR的团队。

第六种玩法是多模型对比。同一个任务，同时跑几个不同的模型，看哪个结果最好最省钱。有强迫症的玩家会这样干：先让GPT-4生成一版，再让Claude生成一版，最后让Gemini生成一版，对比一下选出最优的那个。虽然费token，但能确保结果质量。

说完玩法说说在国内怎么用。最简单是找API转发服务，国内不少创业者做这个生意，人民币付款就能获得稳定endpoint，速度比直连快很多。费用方面，一般是官方价格的1.2到1.5倍，考虑到省下的时间和麻烦，还是值。另外也可以用一些镜像站点，但得挑靠谱的，遇到跑路的就血本无归了。最推荐的还是自己搭个简单代理，完全可控，技术难度不高，搜一下教程就能搭。

用的时候有几个注意事项。一个是数据安全，核心代码最好本地处理一下再给AI，比如把数据库密码、业务敏感信息先脱敏。另一个是模型选择，不是所有模型都适合编程，有的推理能力强但代码能力弱，得自己测试。最后是免费额度悠着点，用脚本批量调用容易被检测异常直接封号，得不偿失。

最后说下为什么推荐这套组合。首先它便宜啊，对于个人开发者来说几乎零成本。其次它开源，数据完全自己控制，不用担心代码被第三方收集。再次它灵活，你可以随意切换模型、修改提示词、定制工作流。最后它持续更新，GitHub上几千个 contributors日夜在维护，好多付费工具都没这个更新速度。

如果你心动了，现在就可以行动。GitHub搜OpenCode，中文教程一堆，装好配好也就十几二十分钟。遇到问题可以在评论区留言，大家一起讨论。这套组合特别适合不想花太多钱在订阅服务上、但想用上最新AI工具的程序员。也适合那些想要完全控制自己数据、不希望代码被第三方收集的开发者。

时代变了，AI编程工具不一定要付费才能好用。开源加免费额度，照样能打出王炸。

---

长按二维码 · 一起聊聊

<img src="https://raw.githubusercontent.com/dashwang/ai-news/main/images/qrcode.png" width="180px">
对了，这套组合还有一个隐藏福利。因为OpenRouter聚合了全球的模型，所以你可以第一时间用到最新发布的模型，不用苦等官方API支持。今天刚发布的模型，明天就能通过OpenRouter用上。这对于那些追求最新技术的玩家来说，简直是白嫖党和技术流的终极选择。赶紧去试试吧。
